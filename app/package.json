{
  "name": "door3-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Door 3 decision tool — 398 music-production master's records and 120 funding schemes",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "calendar": "node scripts/build-calendar.mjs",
    "dev": "npm run data && vite",
    "build": "npm run data && npm run calendar && tsc --noEmit && vite build",
    "build:single": "npm run data && npm run calendar && vite build --mode single",
    "build:all": "npm run build && npm run build:single",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "^10.27.2",
    "preact-iso": "^2.9.2",
    "zustand": "^5.0.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^22.20.1",
    "esbuild": "^0.25.10",
    "typescript": "~5.9.3",
    "vite": "^7.1.7",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^3.2.4"
  }
}
